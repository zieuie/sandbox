# Scratch
This is scratch paper for Sudborough's research.

## Repository Layout
- Solid programs are in `solid/`
- Some results are in `results/`
- Old things are in `archive/`
- Some quick ideas are implemented in `poc/`

## Running Programs
To run Python, consider running with pypy3


To build C++ use:
```
g++ -O3 -std=c++20 filename.cpp
```

or for debugging:
```
g++ -fsanitize=address -g -O3 -std=c++20 filename.cpp
```


